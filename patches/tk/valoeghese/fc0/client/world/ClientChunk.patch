--- a/tk/valoeghese/fc0/client/world/ClientChunk.java
+++ b/tk/valoeghese/fc0/client/world/ClientChunk.java
@@ -30,10 +30,7 @@
         }
         super.setAndUpdateTile(n, n2, n3, by);
-        int n5 = n4;
-        n4 = by;
-        by = (byte)n5;
         ClientChunk clientChunk = this;
         if (clientChunk.renderer != null) {
-            clientChunk.renderer.setTile(by, (byte)n4);
+            clientChunk.renderer.setTile(n4, by);
         }
         if (n == 0) {
