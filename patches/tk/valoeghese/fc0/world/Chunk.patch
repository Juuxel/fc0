--- a/tk/valoeghese/fc0/world/Chunk.java
+++ b/tk/valoeghese/fc0/world/Chunk.java
@@ -50,8 +50,8 @@
 
     @Override
-    public final void setTile(int n, int n2, int n3, byte by) {
+    public final void setTile(int n, int n2, int n3, final byte by) {
         Chunk chunk;
         Chunk chunk2;
-        int n4;
+        final int n4;
         block14: {
             n4 = Chunk.encode(n, n2, n3);
@@ -74,10 +74,7 @@
             }
         }
-        int n5 = n4;
-        n4 = by;
-        by = (byte)n5;
         Chunk chunk3 = this;
         if (chunk3.renderer != null) {
-            chunk3.renderer.setTile(by, (byte)n4);
+            chunk3.renderer.setTile(n4, by);
         }
         if (n == 0) {
@@ -192,7 +189,6 @@
             ((Player)object).chunk = this;
             Player player = object;
-            object = this;
-            if (!((Chunk)object).players.contains(player)) {
-                ((Chunk)object).players.add(player);
+            if (!players.contains(player)) {
+                players.add(player);
             }
         }
